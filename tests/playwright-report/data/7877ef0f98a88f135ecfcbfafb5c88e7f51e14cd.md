# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: login.spec.js >> Blog app >> Login >> succeeds with correct credentials
- Location: e2e\login.spec.js:17:5

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByText('Philemon logged in')
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for getByText('Philemon logged in')

```

```yaml
- heading "Log in to application" [level=2]
- text: username
- textbox: developer
- text: password
- textbox: salainen
- button "login"
```

# Test source

```ts
  1  | const { test, expect, beforeEach, describe } = require("@playwright/test");
  2  | 
  3  | describe("Blog app", () => {
  4  |   beforeEach(async ({ page }) => {
  5  |     await page.goto("http://localhost:5173");
  6  |     await page.evaluate(() => window.localStorage.clear());
  7  |   });
  8  | 
  9  |   test("Login form is shown", async ({ page }) => {
  10 |     await expect(page.getByText("Log in to application")).toBeVisible();
  11 |     await expect(page.locator('input[name="Username"]')).toBeVisible();
  12 |     await expect(page.locator('input[name="Password"]')).toBeVisible();
  13 |     await expect(page.getByRole("button", { name: /login/i })).toBeVisible();
  14 |   });
  15 | 
  16 |   describe("Login", () => {
  17 |     test("succeeds with correct credentials", async ({ page }) => {
  18 |       await page.locator('input[name="Username"]').fill("developer");
  19 |       await page.locator('input[name="Password"]').fill("salainen");
  20 |       await page.getByRole("button", { name: /login/i }).click();
  21 | 
> 22 |       await expect(page.getByText("Philemon logged in")).toBeVisible();
     |                                                          ^ Error: expect(locator).toBeVisible() failed
  23 |     });
  24 | 
  25 |     test("fails with wrong credentials", async ({ page }) => {
  26 |       await page.locator('input[name="Username"]').fill("developer");
  27 |       await page.locator('input[name="Password"]').fill("wrong");
  28 |       await page.getByRole("button", { name: /login/i }).click();
  29 | 
  30 |       await expect(page.getByText("wrong username or password")).toBeVisible();
  31 |     });
  32 |   });
  33 | });
  34 | 
```