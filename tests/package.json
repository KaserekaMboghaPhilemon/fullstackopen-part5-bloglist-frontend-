{
  "name": "bloglist-e2e-tests",
  "version": "1.0.0",
  "description": "E2E tests for blog list application",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
